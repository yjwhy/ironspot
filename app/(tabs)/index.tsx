import { Text, View } from 'react-native';

export default function MapScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-bg-base">
      <Text className="font-bold text-heading-md text-text-primary">지도 화면</Text>
      <Text className="font-sans text-body-sm text-text-secondary mt-2">Task 13에서 구현</Text>
    </View>
  );
}
