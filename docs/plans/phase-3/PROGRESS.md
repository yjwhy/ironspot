# Phase 3 — Implementation Progress

Updated automatically as tasks complete via `/commit-task` command.

## Status

Phase 3 complete. All 8 Tasks (33-40) shipped, PR open for Task 40 verification. Cost: $0 across LLM (Groq + Gemini fallback), STT (native OS), geocoding (Naver Task 28 key), eval workflow (path-filtered, <500 calls/month), and observability (Sentry breadcrumb under 5000 events/month).

### Pre-requisites status (2026-05-13)

- [x] Groq account + API key (2026-05-14, no credit card screen during signup, key in iron-spot-api/.env)
- [x] Google Cloud Gemini API key (2026-05-14, AI Studio auto-created project, no billing, key in iron-spot-api/.env)
- [x] `expo-speech-recognition` package added (Task 38b, PR #79 commit `d746075`)
- [x] First admin user designated via SQL `UPDATE users SET role = 'admin'` (Task 40 live verification flow exercised the admin role toggle via Supabase admin API + SQL)
- [x] Sentry app + api projects (Task 31, live)
- [x] Naver Search API key (Task 28, live)

## Task Checklist

- [x] Task 33: Admin role + 4 admin endpoints
- [x] Task 34: Admin in-app screens (queue + photo detail) + auto-ban cascades
- [x] Task 35: LlmClient abstraction + DSL + prompts + snapshots
- [x] Task 36: NL Search backend pipeline
- [x] Task 37: Rate limit + monthly cron + auth gate + RECORD_AUDIO restoration
- [x] Task 38: NL Search UI + voice input + interpretation chip + map mapping (split into PR 38a backend + PR 38b frontend)
- [x] Task 39: Path-filtered eval workflow + Sentry breadcrumb
- [x] Task 40: Phase 3 final verification + Phase 4 README

## Completed Tasks Log

| 40 | TBD | 2026-05-16 | Phase 3 final verification + Phase 4 README skeleton + operations.md fix-up. **Built as 2 review-gated slices on `task/40-phase-3-verification`**: (a) docs (`docs/plans/phase-3/PROGRESS.md` Task 40 entry + Pre-requisites checklist closed + Task 39 SHA filled; `docs/plans/phase-4/README.md` skeleton with goal/scope/transition notes; `docs/harness/operations.md` hostname fix `ironspot-api.onrender.com → ironspot.onrender.com` + `SUPABASE_JWT_SECRET → SUPABASE_JWKS_URL` row rewrite + GROQ/GEMINI env rows added; memory `project_task39_eval_retry_pending` cleared with retry-result outcome); (b) eval-retry-result chore (Task 39 main retry outcome notes — see below). **9 grilled decisions** locked before code entry (see PR body for full list): (1) voice live verification → Pre-Launch Backlog (Maestro can't drive system mic, EAS preview-simulator deferred), spec deviation #1; (2) admin/user JWT via Supabase admin API + REST signin + SQL role toggle (full autonomy); (3) quota 100/101 → 3-call smoke (Groq TPD constraint + IT 100/101 already covered); (4) admin-flow Maestro → skip (OAuth login bypass infra absent, Jest+IT+live curl cover), spec deviation #2; (5) llm-eval main retry triggered as part of Task 40; (6) Phase 4 README → skeleton (~50 lines, detailed Task plan via Phase 4 grill); (7) supabase.ts MMKV tsc drift → no-op (resolved in main between Task 37 and Task 40, tsc 0); (8) PR slicing → 2-slice (docs / eval-retry-result); (9) Sentry breadcrumb → empty-result query + Sentry events API auto-polling. **Live verification (against `https://ironspot.onrender.com`, autonomous via Supabase admin API + Chrome MCP for Render env fixes)**: test user `task40-verify@ironspot.test` created via `POST /auth/v1/admin/users` + signin → ES256 JWT. **First live attempt revealed two latent prod env issues**: (a) `GROQ_API_KEY` + `GEMINI_API_KEY` were absent on Render (Task 35 added them to backend, never wired to prod); Render startup log: `GROQ_API_KEY not configured — GroqLlamaClient will fail at runtime` + same for Gemini. Added both via Chrome MCP at Render dashboard env tab → save → 4min redeploy. (b) `SUPABASE_JWKS_URL` value on Render was 88 chars, not starting with `https://`, not containing `supabase.co`/`well-known`/`jwks.json` — the legacy HS256 shared secret string was left under the renamed env var key after Supabase migrated to ECC P-256. `NimbusJwtDecoder.withJwkSetUri()` lazy-fetches on first decode, which failed silently and yielded `JwtValidator.validate → Optional.empty() → 401` for every authed request. Fixed by overwriting with `https://ofybwpwicjjtokwqxuxe.supabase.co/auth/v1/.well-known/jwks.json` via Chrome MCP → save → second redeploy. **3-call quota smoke**: PATCH `/rest/v1/users` to set `nl_search_count_month=99` + `role='user'` → `POST /api/search/natural {query, userLat, userLng}` (200, gyms=2, interpretation="강남역 1km 이내에 Panatta 머신 최소 1개씩 보유한 헬스장") → repeat (429, error="이번 달 자연어 검색 한도를 모두 사용했어요. 다음 달 1일에 초기화됩니다.") → PATCH reset → fresh empty-result query in Busan (200, gyms=[], totalCount=0, parsedFilters.brandIds=[Panatta UUID]) confirms quota reset + happy 0-result path. **Admin endpoints curl**: PATCH `/rest/v1/users` to set `role='admin'` → `GET /api/admin/reports?status=pending&limit=5` (200, `[]` — prod DB has no pending reports yet) + `GET /api/admin/photos?status=pending_review` (200, `[]`) + `GET /api/admin/photos/{id}` skipped (no photo id available). The empty arrays prove auth/authz/JSON shape end-to-end — the empty data is the expected prod state, not a verification failure. **Sentry breadcrumb verification**: `POST /api/search/natural` with valid-DSL empty-result query (Busan + Panatta, brand resolves but no gyms in radius) → totalCount=0 → `NlSearchEmptyResultReporter` fires `Sentry.captureMessage("nl_search_empty_result")`. Sentry events API polling failed because `SENTRY_AUTH_TOKEN` scope is `project:releases` (for sourcemap upload) without `event:read`. **Visual confirm via Chrome MCP** at `https://yj-you.sentry.io/issues/?query=nl_search_empty_result&statsPeriod=1h&project=` → issue **IRONSPOT-API-3** Level=Info, 1 event, 1 user, last seen 45s ago. Phase 4 follow-up: regenerate `SENTRY_AUTH_TOKEN` with `event:read` scope or use a separate read-only API token if future automated verification is needed. **Cleanup**: SQL reset `role='user'`, `nl_search_count_month=0`; `DELETE /auth/v1/admin/users/{id}` to remove the test user (auth.users cascade removes public.users row). **Plan deviations** (5, all documented inline + in PR body + Phase 4 README): (1) voice live verification deferred to Pre-Launch Backlog (autonomy constraint + Task 38 manual verification at PR #79); (2) admin-flow Maestro skipped (OAuth login bypass missing); (3) EAS preview-simulator build deferred (Apple Developer enrollment in Pre-Launch Backlog); (4) quota 100/101 reduced to 3-call smoke (Groq TPD + IT redundancy); (5) Render env fixes for `GROQ_API_KEY` + `GEMINI_API_KEY` + `SUPABASE_JWKS_URL` applied via Chrome MCP during Task 40 since they were prereqs blocking live verification (out-of-band but mandatory to honour Task 40's "Live `/api/admin/reports` returns" success criterion). **Maestro E2E**: `pnpm e2e:all` fails on `upload-flow.yaml` + `login-flow.yaml` with "Unknown Property: accessibilityLabel" from Maestro 2.5.1 (selector property name regression separate from Task 40). `nl-search-flow.yaml` runs individually but assertion `"필터" is visible` fails because the live backend's `/api/users/me` 401 (before the Render JWKS fix) puts the app into an indeterminate state; re-run after JWKS fix is part of Phase 4's first Maestro touchpoint. **Local trio**: `pnpm lint` 0, `pnpm exec tsc --noEmit` 0, `pnpm jest` 484/484, `./gradlew test` 282/282. **Task 39 main retry result (slice b)**: `gh workflow run llm-eval.yml -r main -f model=groq` (run 25950954965, dispatched 02:54 UTC during Task 40) cancelled at the 20m timeout after 16/16 attempted cases FAILED with `AssertionFailedError` at `EvalSuiteTest.java:87`. **Root cause attribution at Task 40 close was wrong** (corrected in Task 41 grill, 2026-05-16): line 87 is the **LLM-call catch-path assertion** (`catch (LlmException e) { ...assertThat(false).isTrue() }`), not the `SemanticMatcher` mismatch assertion at line 98. The 75s gap between case failures (60s `RATE_LIMIT_BACKOFF` + 15s `THROTTLE`) confirms `LlmException(RATE_LIMIT)` on both first-attempt and retry. Cases 1-2 actually PASSED (gradle default log surfaces only FAILED case lines); failures begin at case 3 once cumulative tokens approach Groq free-tier TPD (100K/day, hidden from success-response headers — visible only in 429 bodies). **Structural cost**: 30 cases × ~2.5K tokens = 75K/run = 75% of daily TPD. Same-day Groq activity preceding the CI run (Task 35 snapshot recording, Task 39 dev iteration) consumed the remaining 25K headroom, tipping case 3 onwards into RATE_LIMIT. NOT semantic drift. Memory `task39-eval-retry-pending` cleared; new memory `eval-baseline-regression` was created here but later rewritten in Task 41 with the corrected diagnosis. Remediation handled by Task 41: trim suite to 6 product-value cases (~15K/run = 15% of TPD). PR #\__. |
| 39 | 8fb57cc | 2026-05-16 | Path-filtered eval workflow + Sentry breadcrumb (Backend + CI). **Built as 4 review-gated slices on `task/39-llm-eval`**: (a) `2b015db` `feat(phase-3): 39a — SemanticMatcher + queries.yaml eval baseline` — 30 Task-35 snapshot JSONs merged into a single `iron-spot-api/src/test/resources/eval/queries.yaml` after a 1-pass sanity check confirmed every snapshot output already aligned with prompt-policy intent (Korean → English transliteration like `하이로우 → High Row`, slug categories `Chest/Back/Legs`, `gym search only` / `invalid input` error codes). `SemanticMatcher` (test source set, `com.ironspot.search.eval`) implements the per-field tolerance rules grilled in Q9: `radiusKm ±0.2km`, `brand` case-insensitive equality, `machineName` normalized equality (lowercase + strip whitespace) — **not `FuzzyMatchService.jaccard` (plan L1472) because that uses a whitespace tokenizer optimised for OCR text and yields Jaccard=0 on Korean compound words**, `category` exact slug, `minCount/scope/error` exact, `coordinates` skipped (always null at LLM-parse stage). `machineFilters` compared with size-match + order-agnostic element matching. Treats the yaml expected as a complete spec (`expected.brand=null` + `actual.brand=Panatta` → fail). 20 `SemanticMatcherTest` cases covering every tolerance axis + null-handling + order-agnostic + complete-spec. (b) `aa30ab8` `feat(phase-3): 39b — EvalSuiteTest + yaml dep + llm-eval workflow` — added `com.fasterxml.jackson.dataformat:jackson-dataformat-yaml` test dep (snakeyaml is bundled by spring-boot for `application.yml` but it's not a Jackson module — eval needs `SearchDsl` record auto-deserialization). `build.gradle.kts` env-pipe extended: `GROQ_API_KEY` + `GEMINI_API_KEY` join `NAVER_*`in the`.env`-or-process-env pipe so devs can run the eval locally without retyping keys; `EVAL_RUN`is intentionally piped only from process env (never from`.env`) so plain `./gradlew test`never burns Groq quota.`GroqLlamaClient.init()`visibility widened package-private → public (was already manually called by`SnapshotRecorder`in the same package;`EvalSuiteTest`lives in`com.ironspot.search.eval`and needs the same manual-construction path — least-invasive widen vs moving the eval into`.llm`). `EvalSuiteTest`itself:`@EnabledIfEnvironmentVariable(named="EVAL_RUN", matches="true")`(mirrors`NaverSearchServiceIT`), reads `queries.yaml`via classpath,`@ParameterizedTest @MethodSource("cases")`for one method-invocation per case (30 sub-cases on the test report),`SnapshotRecorder`-style throttle (5s between calls + 60s back-off + single retry on Groq `RATE_LIMIT`). Failures collected in a static list and dumped to `build/reports/eval/failures.json`in`@AfterAll`so the workflow can upload the artifact even if the test task itself fails. Hard-fails the parameterized invocation on mismatch so JUnit counts per-case.`.github/workflows/llm-eval.yml`: `pull_request: paths`covering`prompts/`, `search/llm/**`, `search/dsl/**`, `SqlBuilder.java`, `DslValidator.java`, `search/eval/**`(test source), and`resources/eval/**`(queries.yaml) +`workflow_dispatch`with a`model: groq|gemini`choice input reserved for the Q4-future Gemini toggle (Groq-only is the locked default). Same Java 24-daemon-runs-Java-25-compile workaround as`api-ci.yml`. Marker-based comment upsert (`<!-- llm-eval-comment -->`) keeps a single comment across re-pushes; first 3 failures rendered inline with `<details>`collapsibles, full list pointer to the`llm-eval-failures`artifact (90-day retention).`permissions.pull-requests: write`for the comment step. (c)`f30aa08` `feat(phase-3): 39c — NlSearchEmptyResultReporter + Clock bean`— new`ClockConfig` (`@Bean Clock systemUTC()`) so time-sensitive services can be unit-tested with Mockito-controlled `clock.millis()`returns instead of`System.currentTimeMillis()`statics.`NlSearchEmptyResultReporter`(Spring`@Component`, constructor-injected `Clock`) implements the 6-minute swap-clear window grilled in Q5: each unique query within the current window is reported to Sentry once via `Sentry.captureMessage("nl_search_empty_result", scope → scope.setExtra("query", query))`; when the window expires the entire `ConcurrentHashMap`is swap-cleared so the same query becomes eligible again. **Chosen over a Caffeine LRU (no new dep) or a size-capped map (silently drops new queries past the cap, biased reporting) — matches the "10/hour cap per query → 5000/month Sentry budget" reasoning in plan L1404.** First-call`windowStartMs=0`sentinel + double-checked-locking inside`synchronized(this)`avoids a construction-time race where a request lands before`windowStartMs`was initialised. Single-instance Render assumption documented inline (Phase 4 multi-instance path: Redis or per-instance acceptance). 6`NlSearchEmptyResultReporterTest`cases: empty-captures-once, dedup-within-window (3 calls → 1 captureMessage), multi-query-per-window (2 different queries → 2 captureMessages), re-capture-after-7-min, non-empty-totalCount-skipped, null-totalCount-skipped.`MockedStatic<Sentry>`verifies call counts;`mock(Clock.class)`with`when(clock.millis()).thenReturn(...)`drives time deterministically — no flake risk. (d)`d659d84` `feat(phase-3): 39d — wire NlSearchEmptyResultReporter into search`— 7th constructor field on`NlSearchService`, invoked from the existing `finally`block immediately after`recordBreadcrumb`. Reporter receives `(query, totalCount)`on every path (success, BusinessException, RuntimeException —`totalCount`is`null`on failure-before-SQL) and self-filters; uniform finally contract keeps observability layers in lockstep.`NlSearchServiceTest`+2:`emptyResultTriggersReporter`verifies happy-with-empty-list forwards`(query, 0)`; `quotaFailureForwardsNullCountToReporter`pins the failure-path contract by asserting the reporter still receives a call with`totalCount=null`(reporter ignores null — already verified in 39c, not re-verified here). **11 grilled decisions** locked before code (see PR body for full list): (1) branch base —`fix/38b-marker-rendering`was already merged via PR #80, Task 39 forks from clean main; (2) GitHub Secrets —`GROQ_API_KEY`+`GEMINI_API_KEY`registered via`gh secret set`before coding; (3) queries.yaml curation — 30 snapshots merged + sanity-check + no edits; (4) CI LLM client — Groq primary only (not`FallbackLlmClient`) since fallback dynamics already covered by `LlmClientFallbackTest`; (5) Sentry rate-limit — 6-min swap-clear; (6) workflow triggers — `pull_request: paths`+`workflow_dispatch`, cron deferred; (7) PR failure reporting — first-3 comment upsert + full artifact; (8) queries.yaml schema — DSL-shaped; (9) machineName matcher — normalized equality, not Jaccard; (10) test gating — `@EnabledIfEnvironmentVariable`; (11) reporter as separate `@Component`. **Plan deviations** (3, all documented inline + in PR body): (1) `SemanticMatcher.normalize`instead of`FuzzyMatchService.jaccard`(tokenizer mismatch with Korean compound words); (2)`NlSearchEmptyResultReporter`as`@Component`with`Clock`(plan sketched inline in`NlSearchService`with unbounded map — extracted for testability + memory safety); (3)`workflow_dispatch`added to plan's`pull_request`-only trigger. **Test coverage**: +20 `SemanticMatcherTest`+ +30`EvalSuiteTest`parameterized cases (skipped unless`EVAL_RUN=true`) + +6 `NlSearchEmptyResultReporterTest`+ +2`NlSearchServiceTest`. Backend baseline 251 (post-Task-37) → **282 with EvalSuiteTest disabled** (1 skipped), **311 effective when the eval actually runs**. **Verification**: `./gradlew test`282/282 pass with EvalSuiteTest skipped +`EVAL_RUN=true ./gradlew test --tests "*EvalSuiteTest"`end-to-end against real Groq — 30/30 PASS, 216s wallclock (matches 5s throttle × 29 + ~1.5s/call),`failures.json`empty array. Frontend sanity (no FE files touched but ran for paranoia): pnpm trio 484/484 + tsc/lint clean. **$0 cost confirmed**: per PR ~30 Groq calls × 10 LLM-touching PRs/month = 300 calls/month (<2% of Llama 3.3 70B free tier 30K monthly cap); Sentry`nl_search_empty_result` rate-limited to 1/6min/query stays well under 5000 events/month free tier. PR #81. |
| 38 | TBD | 2026-05-15 | NL Search UI + voice input + interpretation chip + 0-result fallback. **Split into PR 38a (backend) + PR 38b (frontend)** after grilling 7 design decisions resolved the SearchFilters multi-select gap that blocked the 0-result fallback. **PR 38a** (`task/38a-backend-multi-select`, 3 commits): (a) `69fd624` `feat(phase-3): 38a — backend filter multi-select + parsedFilters response`—`GymSearchRequest.brandId`/`categoryId`→`brandIds`/`categoryIds` (`List<String>`), `GymRepository.searchInBounds`switches from`.eq()`to`.in()`with empty list =`noCondition`. New `ParsedFilters` record (`search.dto`) — brandIds, categoryIds, templateIds, minCount, scope (minCount/scope nullable for filterless queries). `NlSearchResponse`gains`parsedFilters`; `NlSearchService.toParsedFilters`flattens`ValidatedSearch.filters`(minCount=max across filters, scope from first filter — DslValidator enforces consistency). GymSearchTest: existing cases switched, 2 new cases (multi-brand OR semantics, non-matching exclusion). (b)`905a63f` `chore(phase-3): 38a — regenerate openapi.json + Orval client`. (c) `e7ef8eb` `refactor(phase-3): 38a — adapt gym-search service to array filters`— single-select`brandId`/`categoryId` wrapped into 1-element arrays at the service boundary so main stays green between PR 38a and 38b. **PR 38b** (`task/38b-nl-search-frontend`, 6 commits stacked on 38a): (c) `d746075` `feat(phase-3): 38b — expo-speech-recognition plugin + permission migration`—`pnpm exec expo install expo-speech-recognition`, plugin entry promoted to `[name, config]`form owning microphonePermission + speechRecognitionPermission +`androidSpeechServicePackages: []`. Removes `expo-camera.microphonePermission`(UploadPhotoScreen never used the mic —`takePictureAsync`only) and the hand-rolled`ios.infoPlist.NSSpeechRecognitionUsageDescription`+`android.permissions.RECORD_AUDIO`from Task 37; plugin re-injects both via prebuild. Verified via`pnpm exec expo prebuild --no-install --clean`on both platforms. (d)`939b706` `refactor(phase-3): 38b — widen SearchFilters to multi-select`—`SearchFilters.brandId`/`categoryId`→`readonly string[]` `brandIds`/`categoryIds`; `useFilters.setBrand`/`setCategory`→`toggleBrand`/`toggleCategory`(add when absent, remove when present) + new`setAll()`for`applyFiltersFromNlResult`. `FilterPanel`Chip onPress always emits the chip's id (caller resolves toggle semantics); FilterSectionBody uses`selectedIds.includes(item.id)`so multiple chips can be visually selected.`MapScreen.activeFilterCount = brandIds.length + categoryIds.length`(0–N). 12 tests added/updated covering toggle add/remove/multi-id, setAll, multi-brand forwarding, query-keys, database type. (e)`fe0c432` `feat(phase-3): 38b — search feature infra (MMKV recent history + voice + NL hooks)`—`src/features/search/`: `constants.ts`(RECENT_HISTORY_MAX=10, MIC_SILENCE_TIMEOUT_MS=1000, **MIC_HARD_CAP_TIMEOUT_MS=8000** for silent-input failure mode where partial results never arrive — mic blocked or ambient noise too low — so the silence timer never starts; iOS 60s hard limit otherwise), EXAMPLE_QUERIES seed;`query-keys.ts` `searchKeys.{all,recent,results}`; `lib/recent-storage.ts`(feature-isolated MMKV`id="search-recent"`, versioned key `"nl-search-recent.v1"`, Zod RecentEntrySchema for corruption recovery via `safeParse → []`, Sentry capture on write failure); `useRecentSearches`(LRU dedup, 10-cap, trim-and-skip empty);`useVoiceInput`(wraps expo-speech-recognition with`continuous: true`+ JS 1s silence timer + 8s hard cap — consistent UX vs iOS/Android endpointer differences; VoiceError discriminated by`kind: permission_denied | recognition_failed | silence`; cleanup-on-unmount cancels timers + module.stop()); `useNlSearch`(B-2 signature —`userLat`/`userLng`as props, caller resolves useCurrentLocation fallback to GANGNAM_STATION; 429/400/5xx distinct Korean toasts; onSuccess writes searchKeys.results cache + appends to recent). Jest-safe MMKV mock at`src/test/mocks/react-native-mmkv.ts`. 11 tests (4 recent-storage + 7 useRecentSearches). (f) `9e728f1` `feat(phase-3): 38b — search components (TopSearchBar + mic + history + chip + CTA + badge)`— 6 components:`TopSearchBar`(search input + MicButton + history dropdown on focus + inline X clear),`MicButton`(reanimated pulse + reduceMotion via AccessibilityInfo + Medium haptic on start),`SearchHistoryDropdown`(relative time`방금 전/N분 전/N시간 전/N일 전`, ↖ fill-only + × individual remove with Light haptic + 3s undo toast, 전체 삭제 with Alert confirmation + Warning haptic; empty state shows EXAMPLE_QUERIES with lightbulb icon for onboarding), `InterpretationChip`(rounded pill + 검색 종료 X button — Decision L1 makes the chip the NL-mode exit),`RelaxFiltersCTA`(mid-screen card when totalCount===0),`PermissionDeniedBadge`(영구 banner + Linking.openSettings). 10 component tests. (g)`6734a79` `feat(phase-3): 38b — MapScreen NL Search integration + layout reducer`—`gymsSource` discriminated reducer (`{ kind: 'filter' } \| { kind: 'nl', query, response }`) with two actions (`enter_filter_mode`, `nl_result`). NL response uses camelCase Orval shape so `toGymWithMachineCount`(now exported) maps it before feeding markers + bottom sheet. Layout: top-safe-or-2 + px-4 z-20 (PermissionDeniedBadge conditional → TopSearchBar → InterpretationChip NL-only), FilterButton outside the bar at top-safe-or-2 right-4 z-30, FilterPanel top-safe-or-32 z-40, SearchAreaButton + RelaxFiltersCTA mid-screen at top:'35%' with pointer-events box-none, GymBottomSheet bottom.`applyParsedFiltersAndExitNl`maps brandIds + categoryIds from response.parsedFilters into useFilters.setAll, exits NL mode, opens FilterPanel, and`surfaceDroppedConditions`emits a Korean toast listing what could not be mapped (templateIds → "머신 이름", minCount > 1 → "최소 수량", scope === 'combined' → "동시 보유 조건"). Camera animate-to-NL-center deferred (parsedFilters doesn't carry resolved Location — needs backend extension). (h)`.maestro/flows/nl-search-flow.yaml`(text path: tap input → type → Enter → assert 검색 종료 chip → tap chip → assert chip gone) +`nl-search-voice-flow.yaml`(UI states only — Maestro can't drive system mic). **7 grilled decisions** locked before code (see prior conversation memory): (1)`expo-speech-recognition`plugin single-owner; (2) JS silence timer + 8s hard cap; (3) MapScreen internal reducer + camera animate deferred; (4) GANGNAM_STATION fallback in MapScreen (B-2 props signature for useNlSearch); (5) Chip-as-exit (option L1) + mid-screen floating CTA; (6) parsedFilters → SearchFilters multi-select (option X — required SearchFilters widening); (7) MMKV recent storage with feature-isolated instance + versioned key + Zod validation + LRU dedup + relative-time display + ↖ fill / × undo / Alert-confirmed clear-all. **Test coverage delta**: 458 → 484 frontend tests (+26 across useFilters/FilterPanel/gym-search/query-keys/database type + recent-storage + useRecentSearches + InterpretationChip + RelaxFiltersCTA + PermissionDeniedBadge + SearchHistoryDropdown). Backend remained at 251 after PR 38a's GymSearchTest +2 cases. Pre-existing`supabase.ts`MMKV tsc noise unchanged (Task 37 PROGRESS noted). PR 38a #78, PR 38b #\_\_. |
| 37 | TBD | 2026-05-15 | Rate limit + monthly cron + auth gate + RECORD_AUDIO restoration. **Built as 6 review-gated slices on`task/37-rate-limit-cron-auth`**: (a) `8ff945b`migration — Supabase MCP`add_nl_search_quota`adds`users.nl_search_count_month INT NOT NULL DEFAULT 0`+`users.nl_search_count_reset_at TIMESTAMPTZ`(Task 33 pattern: no Flyway, hand-mirror to`init-test-db.sql`+ JOOQ regen); (b)`c81e260`NlSearchQuotaService — atomic`UPDATE users SET count=+1 WHERE id=? AND count < 100`with race-safe Postgres row-lock +`userService.getOrCreate(principal)`lazy upsert for first-touch users +`@Transactional(propagation = REQUIRES_NEW)`so the increment commits independently of`NlSearchService.search`'s `@Transactional(readOnly = true)`(Postgres rejects writes in readOnly tx); 4-case IT covers under-limit, at-limit, post-reset, first-touch; (c)`dcb1a59`wire-in —`SearchController`adds`@AuthenticationPrincipal UserPrincipal`param,`NlSearchService.search`calls`quotaService.checkAndIncrement(principal)`inside the existing try/finally so a 429 emits`outcome=business_error:429`breadcrumb (gives ops a per-user "hammering at limit" signal);`NlSearchServiceTest`adds quota mock +`quotaExceededShortCircuitsBeforeLlmCall`(verifies LlmClient/DslValidator/SqlBuilder all untouched);`NlSearchControllerIT`adds`@AfterEach resetAuthedUserQuota()`(UPDATE-reset, NOT DELETE —`AUTHED_USER`is shared with AdminControllerIT as`REGULAR_ID`, DELETE would risk FK violations from reports/photos) + new `quotaAtLimitReturns429AndDoesNotInvokeLlm`case; (d)`b6def81`NlSearchQuotaResetJob —`@Scheduled(cron="0 0 0 1 \* ?", zone="Asia/Seoul")`UPDATE-zeros`nl_search_count_month`where`> 0`(predicate keeps it cheap), stamps`nl_search_count_reset_at`to`OffsetDateTime.now(ZoneOffset.UTC)`(canonical storage),`Sentry.captureMessage(INFO)`for ops visibility (12 events/year, well under the 5000-events/month free tier);`@EnableScheduling`on`IronSpotApiApplication`(no other`@Scheduled`methods exist, grep-verified); 3-case IT (zeros every count>0 user, stamps reset_at, skips count=0 rows); (e)`4879995`SecurityConfig —`.requestMatchers("/api/search/natural").authenticated()`listed explicitly alongside`/api/\_admin/**`as defensive insurance (functional no-op since`anyRequest().authenticated()`already covered it from Task 36); (f)`847bc1c`app.json — Android`RECORD_AUDIO`restored (reverses PR #47 — Phase 3 voice STT is a new requirement); iOS`expo-camera.microphonePermission`switched from`false` to a Korean description (`"녹음은 저장되지 않습니다"`for App Store reviewers);`ios.infoPlist.NSSpeechRecognitionUsageDescription`added for`SFSpeechRecognizer`. Verified via `pnpm exec expo prebuild --no-install --platform ios --clean`+`--platform android --clean`. **5 grilled decisions** locked before code entry: (1) `REQUIRES_NEW` propagation for quota service (Postgres readOnly tx incompatibility + matches "no refund on failure" anti-abuse policy); (2) lazy upsert before atomic UPDATE for first-touch users (JwtValidator.findAuthContext defaults mean an authed user can lack a `users` row, which would yield UPDATE-0-rows → spurious 429); (3) Supabase MCP migration + init-test-db.sql hand-mirror (no Flyway in repo, Task 33 pattern); (4) iOS `infoPlist.NSMicrophoneUsageDescription` is owned by `expo-camera` plugin not root — first attempt put it at root and prebuild silently dropped it due to `microphonePermission: false`; (5) Sentry INFO level for monthly reset is safe on free tier. **Plan deviations** (3): (1) `NlSearchQuotaService.checkAndIncrement` takes `UserPrincipal` not `UUID userId` (plan signature) because lazy upsert needs email; (2) quota call moved INSIDE the try/finally (plan Step 3 showed it as the very first call) so the 429 path still emits a breadcrumb under existing observability wiring; (3) UUID range shifts during IT cleanup — `NlSearchQuotaServiceIT` uses `d0000041/42` and `NlSearchQuotaResetJobIT` uses `d0000060-d0000064` to avoid JVM-singleton-Testcontainer collisions with AdminControllerIT (`d0000051-55`, `d0000077`, `d0000099`) and Slack/Sentry smoke ITs. **Code-review fixes applied** across 4 review iterations (one per service slice 37b/c/d): (37b) REQUIRES_NEW + REQUIRED nesting Javadoc clarification, `@AfterEach` cleanup by email suffix (auto-collects principalOf-style users), 99-iteration loop reduced to 3 calls (saves CI seconds), `MONTHLY_LIMIT` tightened from package-private to private, misleading UUID-comment rephrased; (37c) quota call moved into try/finally for breadcrumb coverage, FK-safe UPDATE-reset replaces DELETE on AUTHED_USER cleanup, expanded `verify(...).never()` to cover the whole pipeline (LlmClient + DslValidator + SqlBuilder); (37d) single-instance deployment assumption pinned in a Javadoc paragraph with Phase 4+ remediation (idempotency predicate against `reset_at < date_trunc(...)`), `OffsetDateTime.now(ZoneOffset.UTC)` explicit. **FF review** (4 reviewers) on the lone frontend-touching file (`app.json`): 🟡 predictability — `expo-camera.microphonePermission` string mentions "자연어 검색" while owned by camera plugin (deferred to Task 38, when `expo-speech-recognition` introduces a dedicated owner); 🟡 cohesion — voice permissions spread across 3 declarations (`microphonePermission`, `NSSpeechRecognitionUsageDescription`, `RECORD_AUDIO`) (deferred to Task 38 once STT package's plugin is confirmed). 🔴 0 / 🟢 0. **Test coverage**: NlSearchQuotaServiceIT 4, NlSearchQuotaResetJobIT 3, NlSearchControllerIT +1 (8 total, was 7), NlSearchServiceTest +1 (4 total, was 3); 251 backend tests passing total. Frontend pre-existing `supabase.ts` MMKV tsc/lint errors on main (Expo SDK 55 drift) confirmed pre-existing via `git checkout main` — out of Task 37 scope. PR #77. |
| 36 | c1b9229 | 2026-05-15 | NL Search backend pipeline (Backend). `POST /api/search/natural` wires Task 35 `LlmClient.parse` → `DslValidator` (brand/category lookup case-insensitive via `DSL.upper()`, `FuzzyMatchService.findTemplateIds` for machineName after brand/category pre-filter, COMBINED scope minCount invariant) → `LocationResolver` (Naver top-1 for `NamedPlace`, 0 results → 400, API failure → 502, coords-set passthrough) → `SqlBuilder` (per-filter EXISTS for EACH AND'd, single EXISTS + OR'd matchesFilter for COMBINED, `HAVING SUM(gym_machines.quantity) >= threshold` for both, `ST_DWithin` + `ST_Distance ASC LIMIT 50`) → `InterpretationFormatter` (Korean chip text). **5 grilled decisions** locked before code entry: (1) `SUM(quantity) >= threshold` not `count()` (quantity is physical machine count per row, matching `GymMachineResponse.quantity`); (2) COMBINED threshold = `filters.get(0).minCount()` not sum (LLM prompt rule that combined filters carry same threshold value); (3) strict 400 with helpful Korean on unknown brand / category / machineName-fuzzy-empty (not pass-through, LLM's "never reject" rule is for LLM extraction layer, not backend serve-or-explain layer); (4) brand/category case-insensitive match + machineName Jaccard on `template.name` only (not `brand_name + name`) so brand intent isn't diluted by token overlap; (5) Naver 0 results → 400, API failure → 502 (`NaverSearchService` already wraps), Phase 3 no Naver caching (25k/day free quota >> 100/user/month quota). **Plan deviations** (4): (1) `DslValidator.validate` returns `ValidatedSearch(Location, List<ResolvedFilter>)` with resolved UUIDs/templateIds; plan had void return + SqlBuilder doing inline lookups, splitting keeps SqlBuilder strictly ID-based. (2) New internal records `ResolvedFilter(brandId?, categoryId?, templateIds, minCount, scope)` and `ResolvedLocation(coordinates, radiusKm)`. (3) `SearchController` omits `@AuthenticationPrincipal UserPrincipal user` since principal isn't used until Task 37 rate-limit gate; auth still enforced via Spring Security default chain (verified by `anonymousRequestReturns401`). (4) `FuzzyMatchService` extended with new `findTemplateIds(name, brandId?, categoryId?)` method matching on `template.name` only; existing OCR `findMatches` unchanged. **Code-review fixes applied** (1 🔴 + 4 🟡): (1) per-filter EXISTS aliases use positional index `mt_each_0`, `gm_each_0` instead of `Math.abs(filter.hashCode())` — avoids alias collision on duplicate filters and the `Integer.MIN_VALUE` quirk; (2) `NlSearchService` breadcrumb moved into `try/finally` with `outcome` label `{success | dsl_error | business_error:NNN | runtime_error}` so failure paths emit observability; (3) `translateDslError` default branch returns Korean generic instead of raw English code; (4) `InterpretationFormatter` always renders category in chip text (previously dropped when brand or machineName was also set); (5) `DslValidator` COMBINED-invariant error messages translated to Korean. **Test coverage**: FuzzyMatchServiceTest +6 cases on `findTemplateIds`, DslValidatorTest 10 (brand/category resolution + COMBINED invariants), LocationResolverTest 4, InterpretationFormatterTest 7, NlSearchServiceTest 3, SqlBuilderIT 6 with PostGIS Testcontainer (EACH multi-AND, COMBINED OR-SUM, templateIds, distance order), NlSearchControllerIT 7 (happy / Naver geocoding / unknown brand 400 / LLM 502 / parse error 400 / anonymous 401 / COMBINED scope). 248/248 backend tests passing. OpenAPI re-exported, Orval `search-controller` client generated. Frontend pre-existing `src/shared/lib/supabase.ts` MMKV tsc/lint errors on main (Expo SDK 55 upgrade) noted as out-of-scope. PR #76. |
| 35 | TBD | 2026-05-14 | LlmClient abstraction + DSL types + prompts + snapshot fixtures (Backend). **Built as 5 review-gated slices on `task/35-llm-client-abstraction`**: (a) `f1d777c` DSL + abstraction + fallback test, (b) `3bf4b37` Groq primary + system prompt + config, (c) `e19b84a` Gemini fallback + bean wiring, (d) `09e6672` recordEvalSnapshots Gradle task + recorder, (e) `2414694` 30 fixtures + recorder hardening. **DSL** (`search/dsl/`): `SearchDsl` record with compact-constructor invariants (error/location mutually exclusive; non-error response requires location); sealed `Location` interface with `Current` + `NamedPlace` variants and Jackson `@JsonTypeInfo(property="type")` so `"current"` / `"named_place"` discriminator round-trips; `MachineFilter` record requiring at least one of brand/machineName/category and `minCount >= 1`, scope defaults to EACH if null; `SearchScope` enum with `@JsonCreator` + `@JsonValue` for lowercase wire format (prompt emits `"each"` / `"combined"`, enum stays uppercase in Java); `Coordinates(lat, lng)` record (filled later by Naver geocoder in Task 36). **LLM clients** (`search/llm/`): `LlmClient` interface, `LlmException` **extending `BusinessException`** with kind-to-HTTP mapping (RATE_LIMIT→503, TIMEOUT→504, TRANSPORT/INVALID_RESPONSE→502) so the global exception handler can route 5xx to Sentry without bespoke wiring; `FallbackLlmClient` composite (RATE_LIMIT/TIMEOUT/TRANSPORT fall back with Sentry breadcrumb `category=llm.fallback`, INVALID_RESPONSE propagates without retry); `GroqLlamaClient` calling `api.groq.com/openai/v1/chat/completions` with `llama-3.3-70b-versatile` + `response_format=json_object` + `temperature=0`; `GeminiFlashClient` calling `generativelanguage.googleapis.com/v1beta/models/gemini-flash-lite-latest:generateContent` with `responseMimeType=application/json` (responseSchema deferred — DSL invariants catch malformed responses); `LlmClientConfig` defines all three as `@Bean` (concrete types for the two providers, `LlmClient` interface only on the composite) so `@Autowired LlmClient` resolves unambiguously. **Plan deviations** (3): (1) **Model swap to `gemini-flash-lite-latest`** — implementation.md named `gemini-2.0-flash`, but empirical probe returned `429 RESOURCE_EXHAUSTED` with `limit:0` on the unmonetized Phase 3 project. The `-latest` alias (currently `gemini-3.1-flash-lite`) works on the free tier without re-coding the model name as Google retires lite variants; documented at GeminiFlashClient.java Javadoc + Step 4 of implementation.md. (2) **`responseSchema` deferred** — the `SearchDsl` compact-constructor invariants + Jackson record deserialization already catch malformed LLM output; a JSON schema would double the maintenance cost as the DSL evolves. (3) **Static `MAPPER = new ObjectMapper()`** on both clients — Spring Boot 4 in this project uses Jackson 3 (`tools.jackson.databind`) without a managed `ObjectMapper` bean; mirrors `LogbackProdProfileIT` which does the same. Records work natively for both Jackson 2.x annotations (transitively present) and Jackson 3 databind. **System prompt** (`prompts/search-dsl.md`): 11 Korean few-shot examples driving Korean NL → SearchDsl JSON. Documents brand/machine/category free-text policy: known international names normalized to English (파나타→Panatta, 사이베스→Cybex, 하이로우→High Row), unknown stays literal; default radius 1km when not stated; subjective quantifiers (많은, 충분한) map to minCount=1; SQL keywords / code snippets → error; minimal `헬스장` input defaults to Current(1km, []), NOT error. **recordEvalSnapshots Gradle task** (verification group, pinned to Java 25 toolchain via `javaLauncher.set(javaToolchains.launcherFor(java.toolchain))`): invokes `SnapshotRecorder` main class with `GROQ_API_KEY` piped from env or `.env`. Recorder reads `queries.txt` (30 queries, # comments and blank lines skipped), is **idempotent** (skip when `<sanitized>.json exists`), throttles 5s between requests, and **backs off 60s + single retry on RATE_LIMIT** after a first-run TPM-window storm produced 15/15 ok/err. Second run filled in the 15 first-pass failures cleanly; 30/30 snapshots committed. **Test coverage**: `LlmClientFallbackTest` (6 cases: happy + 3 transient kinds + INVALID_RESPONSE no-fallback + both-fail propagates fallback exception); `GroqLlamaClientTest` (17 cases on `parseContent` + `mapWebClientError`); `GeminiFlashClientTest` (19 cases including Gemini-specific edges: empty candidates, empty parts, non-Map part for multi-modal safety); `SnapshotRecorderTest` (4 cases on filename sanitization). HTTP request body shape is config-only and verified end-to-end by `recordEvalSnapshots` itself (documented in test class Javadoc). **Code-review fixes applied** across 3 review iterations: (1) `LlmException` switched to extend `BusinessException`; (2) `SearchDsl` + `MachineFilter` invariants tightened; (3) `Location.NamedPlace` coordinates nullability documented; (4) empty API key short-circuits to `LlmException(TRANSPORT)` instead of NPE from `setBearerAuth(null)`; (5) Reactor block-timeout string match replaced with Netty `ReadTimeoutException` + `TimeoutException` cause-chain walk; (6) `SearchScope` `@JsonCreator` so the prompt can emit lowercase; (7) one extra COMBINED machineName few-shot added; (8) Gemini test parity gap (empty candidates/parts) + non-Map part guard. 199/199 backend tests passing. Frontend tests untouched (backend-only diff). PR #58. |
| 34 | TBD | 2026-05-13 | Admin in-app screens (queue + photo detail) + backend admin photo endpoints + auto-ban cascades. **Backend**: `UserResponse.role` added with `@Schema(allowableValues={"user","admin","owner"})` so Orval narrows the union → unblocks frontend `AdminGuard`. 3 new admin endpoints (`GET /api/admin/photos?status=pending_review` photo-grouped queue via `mode() WITHIN GROUP` SQL, `GET /api/admin/photos/{id}` detail with `{photo, uploader, pendingReports[]}` aggregate, `PATCH /api/admin/users/{id}/unban` no-UI endpoint). 4 record DTOs (`AdminPhotoSummary` with admin-only `isBlinded`, `AdminUserSummary` PII-minimised, `AdminPhotoDetailResponse`, `AdminQueuePhotoSummary`). **Auto-ban cascade** in same `@Transactional` as `updateDisposition`: actioned → photo.setBlinded(true) + countActionedByUploader ≥ 3 → ban + Slack; dismissed → countDismissedByReporter ≥ 5 → ban + Slack. Asymmetric thresholds because dismissed is a noisier signal than actioned. `markBanned` returns rows-updated for idempotency (no Slack re-fire). 20 new AdminControllerIT cases on top of the 22 from Task 33 (queue, detail, unban, both cascade directions including no-re-fire-when-already-banned). **Plan deviation**: spec said to add role assertion to `MyContentTest` but `/me` is tested in `UserControllerTest` — added the role assertion there as a one-line extension to `getMeCreatesAndReturnsUserOnFirstVisit`. **@AfterEach** added to `AdminControllerIT` after observing that the dispose-actioned cascade blinds `aa000001` and bans `d0000001` (uploader) — that state leaked into `MyContentTest`/`PhotoListTest` which assumed the seeded baseline; the cleanup hook restores baseline so class-boundary leaks don't bite. **Frontend**: `LoginPromptEmptyState` relocated from `features/profile/components/` to `shared/components/` with `description?: string` prop (default text preserved; profile callers unchanged). New `src/features/admin/`: `AdminGuard` (3-way branch — anonymous → LoginPromptEmptyState with admin description, auth non-admin → `권한이 없습니다` EmptyState, admin → children), `AdminQueueScreen` (FlashList of photo-grouped pending photos, oldest first, tap → photo detail, empty state), `AdminPhotoScreen` (photo + isBlinded badge + uploader summary + banned badge + per-report dispose buttons + bottom restore/ban). **Mutation wrappers** following the new frontend-guideline rule: `useDisposeReport` (toast 처리됨/반려됨, invalidate pendingPhotos + photoDetail), `useRestorePhotoAction` (renamed from `useRestorePhoto` to avoid Orval collision, toast 사진 복구됨), `useBanUserAction` (renamed for Orval-collision symmetry, exposes `confirmAndBan()` with Alert.alert destructive confirm, invalidates pendingPhotos + photoDetail). **Code-review fixes** applied (3 🔴 FF findings): (1) `ReportRow.isLast` prop + child-owned `router.back()` → lifted to parent (`Detail`) and self-contained `handleDisposed`; (2) `useBanUser` missing `photoDetail` invalidation → added `photoId` param mirroring `useDisposeReport`; (3) `useBanUser` name collision with Orval `useBanUser` → renamed wrapper to `useBanUserAction` for symmetry with `useRestorePhotoAction`. **FormatRelativeKo** Korean relative-time helper added to `src/shared/lib/format.ts` (방금 전 / N분 전 / N시간 전 / N일 전). **Expo Router**: `app/admin/{_layout,queue,photo/[id]}.tsx` with class-level `AdminGuard` wrap in `_layout`. **Docs**: `frontend-guidelines.md` gained "Mutation 훅은 wrap한다 (기본값)" rule citing AccountSettings as the documented exception. 10 admin frontend tests (AdminGuard 3, AdminQueueScreen 3, useDisposeReport 2, useBanUserAction 2), `MyContentTest` and `PhotoListTest` still green after the @AfterEach addition. Frontend 458/458 + backend 154/154 tests passing. 3 🟡 FF findings (handleDispose API surface, formatRelativeKo location, THUMB_SIZE export) deferred to follow-up. PR #53.
| Task | Commit | Date | Notes |
| ---- | ------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 33 | TBD | 2026-05-13 | Admin role + 4 admin endpoints (Backend). `UserPrincipal` grew `role` (default "user") + `bannedAt` + `isBanned()`; `getAuthorities()` returns `ROLE_ADMIN` / `ROLE_USER` based on `role`. `JwtValidator` gained `UserRepository.findAuthContext()` lookup after JWT parse — user-not-found falls back to `(role="user", bannedAt=null)` so first-touch users (no `users` row yet) still authenticate. `JwtAuthenticationFilter` writes `403 {"error":"banned"}` for `principal.isBanned()` before setting `SecurityContext` (session-level kill, not admin-surface-only). `SecurityConfig`: `@EnableMethodSecurity` enables `@PreAuthorize`; tried adding `requestMatchers("/api/admin/**").hasRole("ADMIN")`for defence-in-depth but it routes denials through`AuthenticationEntryPoint`(401) conflicting with @PreAuthorize 403 — reverted with comment, role gate stays on the controller.`GlobalExceptionHandler`gained`@ExceptionHandler(AccessDeniedException.class)`→ 403 (without it, Spring Security 6's`AuthorizationDeniedException`bubbled to`handleUnexpected`→ 500 + Sentry noise).`AdminController`(4 endpoints, class-level`@PreAuthorize("hasRole('ADMIN')")`): `GET /admin/reports?status&limit`, `PATCH /admin/reports/{id}`(body`{disposition: actioned | dismissed}`validated via`@Pattern`), `PATCH /admin/photos/{id}/restore`, `PATCH /admin/users/{id}/ban`. `AdminService` `@Transactional`with CAS-style updates:`updateDisposition`filters`WHERE status='pending'`so already-disposed reports return 0 rows → 409;`markBanned`filters`WHERE banned_at IS NULL`→ already-banned → 409.`ReportRepository`gained`findByStatusOrderByCreatedAtDesc / findById / existsById / updateDisposition`; `UserRepository`gained`findAuthContext / markBanned`. **`BanRequest`DTO dropped** during code review (plan deferred the reason column to follow-up; accepting an ignored body field is API debt). **22 AdminControllerIT cases**: 4 endpoints × (admin / regular / anonymous) + 9 edge cases (already-disposed 409, missing UUID 404, invalid disposition 400,`disposed_by`+`disposed_at` assertion, status filter, banned session 403 on both admin and non-admin route). **`@PreAuthorize`test gotcha**: Spring Security 6's`AuthorizationDeniedException extends AccessDeniedException`so a single handler at`GlobalExceptionHandler`covers both class-level and method-level denials. **Plan deviations** (3): no Flyway in repo → migration via Supabase MCP`apply_migration`+`init-test-db.sql`hand-mirror (Phase 2 pattern);`UserPrincipal`stayed Lombok`@Builder`not`record`(record would have broken 7 existing`.builder()`call sites); JWT-to-principal DB lookup lives in`JwtValidator`(single seam mockable via`@MockitoBean`) rather than `JwtAuthenticationFilter`. **Supabase migration `add_user_banned_at_and_report_disposition`** applied: `users.banned_at TIMESTAMPTZ NULL`, `idx_users_role`partial index,`reports.disposed_by UUID REFERENCES users(id) NULL`, `reports.disposed_at TIMESTAMPTZ NULL`; `users.role`skipped (already existed with`CHECK IN ('admin','user','owner')`from a prior unrecorded migration — kept as-is, broader than test schema but harmless). **Phase 2 carryover gaps surfaced** (separate section above): prod missing`users.deleted_at`(Task 30 PR shipped code but not schema),`reports.reviewed_at`test-schema drift,`users.role`CHECK divergence — none blocking Task 33. **Code review applied** (4 of 5 items): banned-session test on non-admin route,`disposed_at`assertion,`BanRequest`dropped,`requestMatchers().hasRole()`reverted;`restorePhoto` idempotency test deferred (not plan-required). FF review (4 reviewers): 0 findings (diff 100% backend Java + Orval-generated TS, no hand-written frontend). 22 admin ITs + full backend suite green, 447 frontend tests green, OpenAPI re-exported, Orval client regenerated. PR #\_\_. |

## Pre-Launch Backlog (status snapshot, not Phase 3 numbered Tasks)

App Store submission gates carried from Phase 2:

- [x] **Apple Sign In code** (PR #46) — `LoginScreen` iOS-only Apple button via Web OAuth pattern, deferred external wiring
- [x] **Android RECORD_AUDIO removed** (PR #47) — was correct at the time; restored in Task 37 when voice STT became a requirement
- [ ] **Apple Sign In external wiring** — Apple Developer enrollment + Service ID + Supabase Apple provider + `ios.usesAppleSignIn`
- [ ] **Privacy Policy + Terms of Service** — content + hosted URLs + App Store Connect link
- [ ] **UptimeRobot keep-warm** — optional, 5-min `/actuator/health` ping

## Task 33 → Task 34 absorbed prereq

- **`UserResponse` DTO missing `role`** — Task 33's plan said this DTO would include `role` as a side-effect of adding the column, but the merged PR (#50) did not modify the Java DTO. Without it, the frontend `useCurrentUser` returns a `UserResponse` without `role`, so `AdminGuard` cannot tell admin from regular user. **Absorbed into Task 34 as Step 0**: `UserResponse.role` + `UserRepository.findById` SELECT + `MyContentTest` assertion + OpenAPI/Orval regen happen at the top of Task 34 before the frontend admin scaffold.

## Phase 2 carryover gaps surfaced during Task 33 (not blockers)

Discovered while inspecting prod schema via Supabase MCP `list_tables`:

- **`users.deleted_at` missing in prod** — ~~Phase 2 Task 30 (PR #45 cf5dcbd) added the column to `init-test-db.sql` and shipped `UserRepository.findById` / `markDeleted` referencing it, but the prod schema doesn't have it.~~ **Resolved (verified 2026-05-18 during Phase 4 close audit).** Task 40 live verification at `https://ironspot.onrender.com` ran `POST /api/search/natural` plus `GET /api/admin/reports` with valid JWTs, both of which traverse `JwtValidator.findAuthContext` (`SELECT ... WHERE deleted_at IS NULL`). Both returned 200. A truly missing column would have produced a JOOQ SQL exception → 500. Prod schema has the column; the inspection that surfaced the gap during Task 33 either missed it or it was silently added by a subsequent unrecorded Supabase migration before Task 40.
- **`reports.reviewed_at` exists in prod but missing from `init-test-db.sql`** — purely a test-schema drift; tests won't observe it. Low priority alignment.
- **`reports.status` is a Postgres enum in prod, `TEXT` in test schema** — JOOQ generates `String` from the test schema; prod accepts string-cast values fine, no immediate impact, but test fixtures can't reproduce constraint violation tests for invalid status values.
- **`users.role` CHECK includes `'owner'` in prod but only `('user', 'admin')` in test schema** — test is stricter, no impact on Task 33 since we only insert `'user'`/`'admin'`. Likely Phase 4 (gym owner workflow) artefact.
- **`reports_unique_reporter_target` UNIQUE constraint exists in test schema but not surfaced by prod `list_tables`** — would need direct `pg_constraint` query to confirm. Task 27 plan asserts both should have it.
