# Gym search flow.
# Auto-search fires on the first camera idle (useMapSearch.triggerInitialSearch).
# No manual tap needed — assert seeded gym is visible in the BottomSheet list.
appId: com.ironspot.app
---
- launchApp
- assertVisible: 'Fitness Factory'
