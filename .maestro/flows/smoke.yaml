# Smoke test — runs on every PR.
# Verifies the app boots into the real Map screen (Task 13+) without a
# fatal error. FilterButton accessibilityLabel="필터" is rendered
# synchronously before any network call — reliable sub-30s boot check.
appId: com.ironspot.app
---
- launchApp
- assertVisible: '필터'
- assertNotVisible: 'Error'
