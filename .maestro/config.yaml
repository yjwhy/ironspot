# Workspace config used by `maestro test .maestro/` (full Phase 1 + 2 suite).
# upload-flow, login-flow, upvote-flow rely on a logged-in test account on the simulator
# (Supabase OAuth cannot be automated by Maestro — Task 32 decision #5).
flows:
  - flows/smoke.yaml
  - flows/gym-search.yaml
  - flows/gym-detail.yaml
  - flows/photo-gallery.yaml
  - flows/photo-detail.yaml
  - flows/upload-flow.yaml
  - flows/login-flow.yaml
  - flows/upvote-flow.yaml
